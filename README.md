# Messenger

NexusTeam Messenger is a static UI for login, registration, and messenger chats with sample data. The app ships as a frontend served with nginx inside Docker.

## Run

```powershell
cd Nexus-Team
docker compose up --build
```

Open http://localhost:8080.
